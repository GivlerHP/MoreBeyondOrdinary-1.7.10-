package ru.givler.mbo.network.packet;
import cpw.mods.fml.common.network.simpleimpl.*;import cpw.mods.fml.relauncher.Side;import io.netty.buffer.ByteBuf;import net.minecraft.client.Minecraft;import net.minecraft.entity.player.EntityPlayerMP;import net.minecraft.nbt.*;import net.minecraft.world.World;import ru.givler.mbo.dungeon.*;import ru.givler.mbo.network.*;
public final class PacketDungeonAreaSync implements IMessage{private static final int MAX=4*1024*1024;private int dimension;private byte[] data=new byte[0];public PacketDungeonAreaSync(){}private PacketDungeonAreaSync(World world,DungeonAreaSavedData saved){dimension=world.provider.dimensionId;NBTTagCompound tag=new NBTTagCompound();saved.writeToNBT(tag);try{data=CompressedStreamTools.compress(tag);}catch(Exception ignored){}}public static void broadcast(World world,DungeonAreaSavedData saved){PacketManager.INSTANCE.sendToDimension(new PacketDungeonAreaSync(world,saved),world.provider.dimensionId);}public static void send(EntityPlayerMP player){PacketManager.INSTANCE.sendTo(new PacketDungeonAreaSync(player.worldObj,DungeonAreaSavedData.get(player.worldObj)),player);}@Override public void fromBytes(ByteBuf b){dimension=b.readInt();int n=b.readInt();if(n>0&&n<=MAX&&n<=b.readableBytes()){data=new byte[n];b.readBytes(data);}}@Override public void toBytes(ByteBuf b){b.writeInt(dimension);b.writeInt(data.length);b.writeBytes(data);}public static final class Handler implements IMessageHandler<PacketDungeonAreaSync,IMessage>{@Override public IMessage onMessage(final PacketDungeonAreaSync m,MessageContext c){EditorPacketAccess.scheduleClient(new Runnable(){public void run(){try{if(Minecraft.getMinecraft().theWorld!=null&&Minecraft.getMinecraft().theWorld.provider.dimensionId==m.dimension)ClientDungeonAreas.read(m.dimension,CompressedStreamTools.func_152457_a(m.data,new NBTSizeTracker(MAX)));}catch(Exception ignored){}}});return null;}}}
