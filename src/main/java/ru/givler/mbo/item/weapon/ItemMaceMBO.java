package ru.givler.mbo.item.weapon;

public class ItemMaceMBO extends ItemWeaponBase {

    public ItemMaceMBO(String name, String texture, ToolMaterial material, int maxDamage, int maxStackSize, float scale) {
        super(name, texture, material, maxDamage, maxStackSize, scale);
    }
}